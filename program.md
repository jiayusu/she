autoprogram
This is an instruction to have the LLM autonomously complete a project build.

适用对象
任何有明确规格书（SPEC）的可构建产物：网站、App、硬件固件。
核心原则：人类写规格（立法），AI 负责把规格变成可运行的产品（行政）。
你的任务不是"尝试"，是"交付"——以 SPEC 全部验收项通过为唯一完成标准。

================================================================
Setup（开工前，与人类协作完成）
================================================================

1. Agree on a run tag: 按日期提议（如 sep7），分支 autoprogram/&lt;tag&gt; 不得已存在。
   git checkout -b autoprogram/&lt;tag&gt;
2. 通读仓库全部在范围内的文件：
   - SPEC.md   — 产品规格：功能清单、交互流程、验收标准。冻结，不可改。
                 若验收标准含糊，在 DEVLOG.md 中记录你的解读，继续推进，
                 不要停下来等澄清（除非缺失会导致方向性错误）。
   - BUILD.md  — 构建/烧录/打包/部署命令。
   - 现有源码   — 全部读一遍再动手。不存在"先改了再说"。
3. 验证工具链可用（构建命令能跑通、开发板能连上、签名/证书就位）。
   环境缺失 → 明确告诉人类缺什么，列成清单，暂停等待。
   这是唯一允许暂停等待人类的场景。
4. 写 PLAN.md（工作目录下，不入 git）：把 SPEC 拆成可独立验证的任务项，
   每项标注验收方式。按「风险从高到低、依赖从底到上」排序。
5. 初始化 DEVLOG.md（入 git，每个任务一次提交对应一条记录）。
6. 确认开工。

================================================================
What you CAN do
================================================================

- 实现 SPEC.md 中的一切：页面、接口对接、状态管理、端侧推理、驱动、
  通信协议、UI、动效、数据持久化。
- 自主选择技术方案与内部架构（库仅限依赖清单中已有的）。
- 主动补齐 SPEC 未明说但产品必然需要的部分：
  空状态、加载态、错误提示、边界输入、断电恢复、弱网重试。
  —— 此类决策记入 DEVLOG，标注 "SPEC 外推断"。
- 发现 SPEC 内部矛盾时：采用更保守、更可验证的那一版，DEVLOG 记录矛盾点。

What you CANNOT do
================================================================

- 修改 SPEC.md / BUILD.md。
- 删除或弱化任何一条验收标准来"完成"项目。
- 安装新依赖、引入新工具链。
- 用 mock 数据冒充真实功能通过验收（开发期 mock 可以，交付前必须接通真实链路）。
- 对硬件：跳过任何上电自检；用注释掉代码的方式规避硬件故障。
- 提交无法构建的代码（每次 commit 前必须本地构建通过）。

================================================================
The build loop
================================================================

LOOP UNTIL SPEC 全部验收项通过:

1. 从 PLAN.md 取下一个未完成任务。
2. 实现 → 构建 → 按该项的验收方式自测。
3. 构建失败：读报错，修。低级错误当场修，不记入失败。
4. 验收不通过：分析根因，修。同一任务卡壳超过 3 次 → DEVLOG 记录
   卡壳原因与当前假设，转做下一个不依赖它的任务，回头再战。
5. 通过 → git commit（一条任务一个 commit，commit message 写清
   "实现了什么 + 如何验证的"）→ DEVLOG 追加一条 → 下一个任务。
6. 每完成 3-5 个任务：跑一次全量构建 + 全量自测，防止集成退化。

硬件特有流程：
  每实现一个功能 → 烧录 → 串口日志确认 → 真实外设动作确认
  （灯真的亮了、舵机真的转了、麦克风真的有数据）。
  "日志看起来对" ≠ "硬件真的工作了"。一切以物理世界现象为准。

App 特有流程：
  release 构建 + 真机或模拟器跑通主流程才算数，debug 构建通过不算。

网站特有流程：
  生产构建 + 无头浏览器跑通核心用户路径（含错误路径）。

================================================================
DEVLOG.md 格式
================================================================

## [时间] 任务: &lt;PLAN 任务名&gt;
- 实现: &lt;一句话&gt;
- 验证: &lt;跑过什么命令/什么物理现象，结果&gt;
- SPEC 外推断: &lt;若有，写明推断依据&gt;
- 遗留: &lt;若有，写明&gt;

每完成一个功能必写。DEVLOG 是交接时人类理解"AI 到底做了什么"的
唯一依据，不许省。

================================================================
Crashes & 阻塞
================================================================

- 笔误/漏 import/漏文件 → 当场修，不记录为事故。
- 环境/物理问题（板子损坏、证书过期、依赖源挂掉）→ 明确记录现象、
  已排除的原因、需要人类做什么。然后转做不依赖该环境的任务。
- SPEC 与物理现实冲突（如规格要求的采样率超出传感器能力）→ DEVLOG
  记录冲突细节，按"传感器实际能力上限"实现，醒目标注待人类裁决。
  不要假装规格实现了。

================================================================
Done 的定义
================================================================

同时满足：
1. SPEC.md 验收清单逐项勾选，每条附验证证据（命令输出 / 截图路径 /
   串口日志摘录）。
2. 全量构建从零开始可复现（干净 checkout → BUILD.md 命令 → 产物）。
3. DEVLOG.md 完整，SPEC 外推断与遗留问题全部标注。
4. 向人类输出交付摘要：实现了什么、怎么验证的、SPEC 外推断有哪些、
   遗留问题清单。

NEVER STOP: 开工后不得停下来问"这样行吗""继续吗"。人类可能已离开，
期望回来时看到成品。所有决策自己承担，所有判断写入 DEVLOG。
唯一的例外是 Setup 第 3 步的环境缺失——那是唯一允许暂停的理由。

================================================================
三形态差异速查
================================================================

              网站                App                  硬件
完成标准      构建+核心路径可走     release 包+主流程真机通过  全功能物理现象确认
构建命令      npm run build        flutter build apk        idf.py build
自测方式      无头浏览器            模拟器/真机脚本           串口日志+物理观察
特有验收      生产构建无 console 报错  冷启动+主要页面帧率      连续运行 30min 无复位
特有禁忌      dev server 冒充交付    debug 包冒充 release     日志正确代替物理动作
================================================================