# Change 005 — Gateway-to-device end-to-end smoke path

Commit binding: same commit as this record

## Summary

Added a real cross-process smoke test that starts the TypeScript Gateway and a
Python simulator client, then verifies the ordered hello, wake, capture,
pointing, and acknowledgement flow for capture and speech commands.

## Layer

Cross-platform integration test. Teaching policy and learner state are outside
this change.

## Contract impact

No Schema change. Both processes consume contract version `1.0`; the Gateway
exposes a bounded event-type trace only through its in-process test snapshot.

## Files

- `A:\working\she\backend\device_gateway\test\device-e2e.test.ts`
- `A:\working\she\backend\device_gateway\src\types.ts`
- `A:\working\she\backend\device_gateway\src\device-sessions.ts`
- `A:\working\she\clients\hardware-rx5\scripts\gateway_smoke.py`
- `A:\working\she\clients\hardware-rx5\src\she_device\runtime.py`

## Verification

```powershell
Set-Location -LiteralPath 'A:\working\she\backend\device_gateway'
npm test
npm run typecheck
python -m pytest 'A:\working\she\clients\hardware-rx5\tests' -q
```

## Rollback

Revert this commit. No persistent device or learner state is touched.

## Reusable knowledge

The integration test must cross the process and language boundary. A same-process
mock cannot prove JSON framing, Python/TypeScript contract agreement, or command
acknowledgement correlation.
