# Change: establish repository governance and engineering memory

**Commit binding:** Same commit as this record.

## Summary

The previously unversioned project gains safe ignore rules, structured playbooks, reusable project skills, and mandatory repository workflow rules.

## Layer

Repository.

## Contract impact

None.

## Files

- `A:\working\she\.gitignore`
- `A:\working\she\AGENTS.md`
- `A:\working\she\playbooks`
- `A:\working\she\skills`

## Verification

- `she-ios-soft-orbit` no-skill baseline chose a raster pet and the wrong third Today metric (`复习完成度`).
- With the skill loaded, the same evaluator selected a SwiftUI-drawn pet, `新词`, the exact Pet First order, and explicit reduced-transparency behavior.
- `quick_validate.py` returned `Skill is valid!` under `PYTHONUTF8=1`.
- Run the absolute-path ignore, placeholder, staged-file, secret, and large-file checks in the implementation plan.

## Rollback

Revert this commit. Local ignored data remains on disk because the change never deletes generated or private files.

## Reusable knowledge

`she-ios-soft-orbit` captures project-specific visual knowledge that the no-skill baseline missed: SwiftUI-drawn pet assets, the exact three Today metrics, palette, and material fallbacks.

No-skill pressure tests already preserved contract-first order and systematic incident recovery under deadline pressure. Those two workflows remain automated checks and repository rules rather than redundant skills. RDK preflight remains a playbook until real hardware verifies it.
