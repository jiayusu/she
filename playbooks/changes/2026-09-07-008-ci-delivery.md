# Change 008 — CI and GitHub delivery

Commit binding: same commit as this record

## Summary

Added fail-fast local verification, tracked-file safety auditing, Ubuntu and
macOS CI, root architecture/quick-start documentation, and a non-force GitHub
release playbook.

## Layer

Repository delivery infrastructure and documentation.

## Contract impact

No contract shape changed. CI verifies canonical fixtures across Python,
TypeScript, and Swift consumers.

## Files

- `scripts/verify.ps1`
- `scripts/audit_repository.ps1`
- `.github/workflows/ci.yml`
- `README.md`
- `playbooks/releases/github-first-release.md`
- `playbooks/changes/2026-09-07-008-ci-delivery.md`

## Verification

Run both absolute-path scripts, `git diff --check`, and repository status. macOS
iOS build/XCTest is intentionally performed by CI because the local host is
Windows.

## Rollback

Revert this commit. Do not force-push or rewrite remote history.

## Reusable knowledge

A multi-command shell transcript is not a reliable gate unless every native exit
code is checked. The repository verifier names platform skips explicitly; the
auditor examines only tracked files so local user data is never read or printed.
